import "../css/style.css"; //thanks to the loaders of css of Webpack we can import the file to a module

const products = ["p1", "p2", "p3"];
console.log(products);
console.log("Javascript file with css file imported!");
